import 'package:flutter/material.dart';

class SearchBarSearchIcon extends StatelessWidget {
  const SearchBarSearchIcon({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.search,
      size: 24,
    );
  }
}
